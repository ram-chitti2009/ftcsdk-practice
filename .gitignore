# Ignore everything at repo root
*

# But keep directories so we can whitelist a specific folder
!*/

# Whitelist TeamCode directory and everything inside it
!TeamCode/
!TeamCode/**

# Keep this gitignore file tracked
!.gitignore

# (Optional) If you want to keep other files tracked, add specific !patterns below
# Example: to keep README.md tracked, uncomment the next line
# !README.md
